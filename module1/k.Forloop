class Geeks {
    static void isPrime(int n) {
        int f=0;
        for (int i = 2; i <= Math.sqrt(n); i++) {
            if(n%i==0)
            {
                f=1;
                System.out.println("No");
                break;
            }
        }
        if(n>=2 && f==0)
        System.out.println("Yes");
        else if(n<2)
                System.out.println("No");

    }
}
